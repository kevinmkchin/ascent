
enum Mutation {
    // Each mutation is defined as: (increase in attack power, increase in health, increase in velocity)
    Mutation1 = (100, 30, 5),
    Mutation2 = (20, 50, 10),
    Mutation3 = (30, 2, 100),
};